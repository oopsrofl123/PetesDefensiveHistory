BUGS
===========================================================================
- disable arcane surge by default

- freedom that never fades

- slotToFrame() = nil failure in trackerui:571

SHADOWMELD
- seems to still be some erroneous shadowmelds. implement group drop protection
    - done. needs testing
- bug on open world and on dummies in silvermoon
    - somehow there was no buff?
    - target dummies do not allow combat to drop if you are close enough to them
        - this is probably to help prevent annoying things like damage meters resetting on vanish
          or something.
    - but still lost meld on an outdoors mob because the flags event happened 0.1s away from
      the aura event.
    - might need a more lax tolerance for meld. leaving combat could be handled differently on the
      server side from simple aura applications

- there is a scenario where the visual debugging 'partyX' label does not show up on a frame
    - happened once in an LFG heroic and one more time in random testing, but I can't make it happen again
    - this did not affect the actual tracker UI weirdly enough, even though the visual debugging label
      is attached to it

* even if the recent change to setSpecAndTalents() fixed the error below, need to test 
  exactly what order events occur when zoning into a dungeon
    - the LFG system is probably a little different
- bug in reading classFile in heroic dungeon
    GroupData.lua: 139: bad argument #1 to 'pairs' (table expected, got nil)
    ..setSpecAndTalents()
    (i.e.,  through libspec)

    self = table {
    }
    spec = 104
    tstring = "talents.."
    specId = 104
    _ = 104
    specName = "Guardian"
    className = "Druid"
    talentExportString = "talents.."
    classFile = nil
    talentRanks = {
        102401=1,
        ...
    }
    abilities = {
    }
    possibleAbilities = {
    }
    UID = "Player-3725-0C5D2422"
    englishRaceName = nil


- new bug in follower dungeon:
    - reload UI
    - immediately cast freedom
    - somehow this circumvents the glow/cooldown swipe



V1.0 RELEASE FEATURES
===========================================================================
- some amount of export support. otherwise there's just no way to know how well it works

- use the new export functionality to export raw events from a few dungeon runs


- watch m0 vod to find bugs
    - why was freedom often in history tray? (was it on non-libspec players?)

- update github/curseforge readmes
    - mention restricted license for now with intended open license later
    - want to work with frontend UI designers


- full pass on ability testing, especially with updating. not easy to do since seemingly uninteresting
  talents can cause updating.  see, e.g., the mage hero talent that causes ice cold/ice block to constantly
  update as they allow you to move
****** CHECK UPDATES
****** CHECK CONCURRENTBUFF ABILITIES - SEE TalentDb - ONLY SHADOW DANCE HAS A non-talented CONCURRENT BUFF
                 natural update?     update when press?      extend?
    - dk
    - dh
blur             false               true                    false
vdh meta
    - druid
celestial alignment
incarn elune
    - evoker
obsidian scales                      true                    false
    - hunter
survival fittest
time dilation
    - mage
ice cold (would snap allow a recast? or prevented by hypothermia?)
    - monk
    - paladin
goak/golden valkyr
    - priest
pain supp
    - rogue
    - shaman
    - warlock
    - warrior
avatar (proc>press)



NOTES ON COMPLEX CASES
    - [UPDATE] meta vs. apex is solved. now cheat death (should be easier)
    - AVATAR SHOULD BE HANDLED WITH UNCERTAIN=true
        - [UPDATE] just guessing that avatar on add is the real avatar is a decent approx. when proc>avatar
          happens, the potential differentiating signals are (a) total duration and (b) an update that happens
          "too fast". avatar updates for two reasons:
            * real avatar was pressed during proc avatar
            * tblast was pressed
          tblast has a 6s (reduced by haste) cooldown, which is where (b)'s "too fast" comes from. (a) can
          also be calculated but will be confused by things like bloodlust, which will allow many more
          avatar extensions than usual due to reduced tblast cooldown. [FOR NOW] treat avatar like every
          other extension ability, guessing that the ADD event is real avatar. as long as stand-alone proc
          avatars are not confused for real avatar, this approximation won't be noticeable since avatar has
          CDR anyway.
        - can't tell whether the proc or real buff was pressed until duration exceeds 4s.
          but it is unambiguous which buff is active, so just light it up immediately.
        - the AotS proc can't happen during avatar
        - is avatar of the storm properly tracked? needs testing
            - cast at 939.946
            - proc > avatar overwrites duration (sets to 20s), doesn't extend
            - avatar can update for 2 reasons: proc, +2s extension
            - if there is a proc, it must be the AURA(add) event
            - but if so, is it possible to determine which of the updates was the real avatar?
                - sad part is even if this can be figured out, avatar has CDR anyway so the timer won't be correct
            - avatar procs don't grant 2 thunder blast stacks
    - THE VDH META SPECIAL CASE: when meta extends, which ability was pressed first?
      this determines the cooldown. when apex>meta, the duration does not
      extend. when meta>apex, it does. so:
        t0=0: buff 1
        t1:   buff 2 (=meta buff is updated)
            if t1: >10s? then t0=meta, t1=apex  
            else:  <10s? if total duration = 30s, then t0=meta, t1=apex
                         if total duration < 30s, then t0=apex, t1=meta
        - Other weird observation: after putting meta on CD unambiguously and then
          procing the apex talent well after meta was over, meta was still considered off
          cooldown for some reason. This bug may have been an issue with not calling
          queueCooldown and is already fixed.
    certain=true
        [apex > meta]    doesn't happen
        meta > apex      don't do anything, don't infer on update. it is convinced meta
                         was cast and it is correct about the timing. it can ignore the
                         apex talent because it's just a meta extension
    certain=false
        apex > meta      duration < 30
        meta > apex      duration = 30

    - THE GIFT OF THE GOLDEN VALKYR SPECIAL CASE: gift of the golden valkyr can update
      goak. this should be easy because of the concurrent debuff
        - because this is recorded as an update to the previous goak
        - if goak is active -> cheat procs, goak never gets deglowed
            - goak gets overwritten by cheat on update because it's >1 charge
        - goak is usually IDed fast (certain=true) because only cheat matches flags and cheat requires debuff
            - should cheat return goak, certain=false?
        - cheat also often IDs since its only flag match is goak and cheat doesn't require a button press
        - overwrite/extend:
            - cheat > goak: overwrite (becomes 8s)
            - goak > cheat: weird extend (becomes 12s no matter goak's dur, seems like a bug)
            - goak > goak: overwrite (becomes 8s)
        - ok, so then these should almost always resolve correctly because of certainty. the only
          chance for messing up is a random debuff landing within tolerance or happening to press
          a button within tolerance of cheat
        - cheat does not glow goak. so it also never deglows goak. seems glowing goak is the better thing
          to do, similar to that dh talent
        MORE NOTES FROM A DIFFERENT DISCUSSION ----------------
        - in uncertain cases: C=cheat, G=GoAK
            CASE: C>G where G cast is close enough to C proc that can't determine which was which
                  need a VDH-like "return GoAK even though it could be cheat"
                  if uncertain, then the two abilities were used so close together that we can't
                  discern which caused the debuff. so just assign arbitrarily. first can always
                  be GoAK. the CD will be off by fractions of a second.
            ^^^ this won't work because we can't distinguish C>G from G>G without C's duration.
                a debuff is not POSITIVE evidence of C - but lack of a debuff is POSITIVE evidence of G by excluding C
                no spellcast is POSITIVE evidence of C - excludes G, leaving only C
                REQUIRES CONSUMPTION TO AUTOMATICALLY RESOLVE (ARBITRARILY)





NEEDS TESTING
===========================================================================
- hides chubb's micromenu (bags, character sheet, group finder, etc. buttons)
    - if it helps: it's from dominos, not the blizz default menu
- better handling of spec querying
	- libspecialization updates during m+ runs. need to figure out why
- groups with sac and ironbark
- warrior avatar procs
- test rider of the apocalypse AMS proc. does it give the same AMS buff? if so
  we might need to catch it. has 45s cd. might have identical duration? might
  not be able to differentiate except by isButtonPress





V1.1 RELEASE FEATURES
===========================================================================
TEST HARNESS
    - wrap the very few WoW API calls with functions that can inject correct responses when run
      in standalone mode
    - if this is possible, could feed public logs into harness to test on massive amounts of data
    - figure out a way to run those raw events through the addon lua code for inference
        - playback() in each handler mostly works, but need wrappers that defer to internally-stored
          data for:
            * UnitAffectingCombat()
            * fasterGetFilterFlagsForAuraInstanceId()
            * GetTime()
        - also need a way to set up trackedCharacters
    - assess accuracy through an accompanying log
        - there will probably be issues with ability ID mapping for those abilities with
          several possible IDs. the IDs are necessary for the extraction step

EXPORTING
    * ALIGNER:
        - appliesOtherAuraId should not work but appears to
            - turns out the cheat death version of GoAK (393108) is in the log alongside normal GoAK
              (86659). this might not apply to other overwrites (maybe even related to 12s cheat bug?)
    * NEEDS TESTING: can the combat log come from someone other than the addon exporter? the decoder
      script supports mapping to the correct slot (i.e., vis-a-vis the addon exporter) but the log and
      export timestamps might be more discordant
    * MUST DO: add option to compartment menu to reset the internal data trackers
        - need this to allow resetting when entering a new instance, e.g.
        - or could do it on ZONE_AREA_CHANGED?
    * would be nice to log encounter IDs/zone IDs somehow. the current ones at the time of export aren't
      necessarily informative
        - right now, collecting data over several m+ runs likely won't work. aligning it to the combat log
          will probably fail. instead, export data needs to be organized by encounter/zone ID. this would
          be pretty clean in raids/m+ but in open world/non-m+ dungeons there is no "start event"
        - would need to key the inference/playback event lists by encounter/zone ID
        - would also have to copy the chardata/metadata per encounter/zone ID
    - finish work on InferenceRecord (logic, conf, reqs, metadata: timestamps, source/aura, ID, traces, etc)
        * add evidence? maybe not necessary
    - add handler triggers to data
        - might require handler collapsing especially for crazy ones like absorb
    - make record type to distinguish inference records from handler events
    - for combat log parsing -> creating tracked characters, have to parse COMBATANT_INFO, which
      fires when starting a key. not sure if anything fires for m0.
        - https://warcraft.wiki.gg/wiki/COMBAT_LOG_EVENT#COMBATANT_INFO
    * have to encode slot=player GUID in playback output for log alignment
        - in real code would encode the whole trackedCharacters(), but myGUID isn't that much to add
    * need to encode ability.caster in InferenceRecord output
    * could logs help tune the time tolerances?
        - motivated by how close log and playback spellcast times are. if aura applications were
          similar, then could get some distributions on tolerances

- char:setBasicInfo() is now updated regularly, but will not do expected things like add racials
  to the ability list because abilities are only added in setSpecAndTalents(), which is not called
  regularly (for good reason).

- EVENT ARCHITECTURE add :consume(), consider moving finalizeInference/trackCooldown to Event
- :consume() - event > ability inferences should somtimes consume the evidence associated with them.
               this essentially assigns evidence to an event.
  there must be a no-side effects version of consume
  PROBLEM: ringbuffer FIFO is a bad data structure for removing arbitrary elements
    - rather than remove, set the item to nil or whatever the initializer element was?
    - solution: ringbuffer becomes 2-tuples of (timestamp, #events), solving 2 problems:
        a. can squish multiple simultaneous events into a single record, reducing the need for
           a large queue size
        b. can set #events=0 when they're consumed and leave the count=0 records in the ringbuffer
  Examples:
    a. must consume: GoAK vs. cheat. there are two relevant evidentiary events: a spellcast and a debuff.
       if GoAK is used at the same time cheat procs, these events are too close to the buff add/updates
       to use them to differentiate which happened first. but, because they're so close together, it doesn't
       really matter which happened first, just make an assignment and move on and tolerate a <0.1s inaccuracy
       in the CD tracker.
       TO DO THIS: either: GoAK *consumes* the spellcast event when it is assigned or cheat *consumes* the
       debuff when it is assigned. consumption means the evidentiary event is no longer present in a followup
       inference.
    b. must not consume: AoE CCs. an AoE CC cast creates one spellcast event but may cause many CCs. the point
       is that consumption is not always correct


- arena: GetArenaOpponentSpec() and GetNumArenaOpponentSpecs() to get specs. doubt we can get talents.

- event handler output collapsing
    - or as a bandaid fix, just mute ABSORB

- what to do with double freedom? the second freedom's caster is known (assuming only 1 paladin)
  but only one can be finalized since it will put freedom on cd.
    - the one that isn't finalized will be continually inferred (with failure), is a very common
      flagset (10000), and will be put in the tray.
    - special handling around cooldown exclusion is necessary to solve

- addon show/hide is a hack right now but happens to work. need to:
    1. delegate showing/hiding responsibility to the TrackerUI update* functions
    2. get rid of extra :Show() loop in respondToRosterUpdate()
    3. consider whether data updating should remain tied to UI updating (respondToRosterUpdate())

- UNIT_FLAGS fires on dismount (and fires an AURA)

- convert evidence trackers from timestamps -> (timestamp, count)
    - squash multiple events at same (or within tol ~1ms) into a single event to avoid
      overflowing the ringbuffer.
    - UNIT_ABSORB is a particularly bad offender, e.g. on prot paladin throwing divine toll grants
      an absorb for every shield hit.

- meld protection: check for combat drops of other players. if too many others also left combat,
  then prevent inferring meld
    - example of meld FP in heroic nexus point at 12:34

- rather than polling, just queue a callback for TOLERANCE seconds in the future

- batch inference
    - when multiple events attached to same ability, perform inference on the batch.
      this generalizes the case of abilities that update themselves without button presses
      (i.e., the old numUpdates data, which was never used) and also is the basis for complex
      chains of aura updates like vdh meta and goak
    - for logging: must print batch ID in addition to event ID
    - when none of the abilities in a batch are certain, only add 1 copy of the secret texture to history tray
    - avatar
        - avatar proc can't occur during avatar, meaning two things:
            a. no procs during avatar
            b. no procs during PROCS
        - every tblast is an aura update and a new event, will be LOTS of events in this batch
        - tblast has the same cooldown as tclap (6s, scales with haste)
            - this constrains number of extensions for both AotS and avatar.
            - AotS: 6s base, 10s max (2 exts): extension 1 -> 8s allowing for cd to finish and cast 2nd
                * need >50% haste to get a 3rd. prob happens during lust
        - since each update = new event, every update that isn't IDed will get sent to the history tray
          batch logic needs prune out extra events to prevent this
            - similar to VDH meta where the apex talent cd isn't tracked, AotS' cooldown also is untracked
            - so the batch logic just needs to determine if the event was long enough for a real avatar to
              put on cd. unfortunately it probably can't be determined whether avatar occurred on the
              AURA(add) or was a proc>avatar. so the exact start of the CD often won't be possible. can take
              the worst case approach and always assume first. an exact CD timer won't matter since there is
              dynamic CDR anyway.
    - goak
    - vdh meta
    - sentinel
        - this is already handled but in a hamfisted way. prevented from AURA(update) by charges=1
          and naturallyUpdates=true. which is another way of saying updates aren't of interest.
        - just need to prevent this from filling up the history tray. not a complex ability

- make certain evidence queryable without going through infer
    - example: player pressed a button. important for making updates generate events

- make a better "isOffCooldown()" function for use in inference engine and in the update aura loop.
  incorporate heuristics for dynamic CDR + charge abilities.

- [MAYBE FALSE REPORT] cooldown visual state is forgotten between zone loads
  internal data in cdtracker is not forgotten, so should be possible to correct.

- WoW BUGS TO HANDLE:
    - VDH meta: procing last resort puts meta on CD
    - generalize the BoP puts spellward on CD subroutine
        - affecting other cooldowns: maybe when BoP is detected, generate a new event with
          the same auraID but a certain ability ID and ability set to spellward?
            - would not work with evidence consumption
            - would possibly glow the spellward icon


- exclamation badges: visual feedback to the user when cooldown tracking (e.g. CDR or
  possibly other reasons) is inconclusive.

- add Vanish ability for rogues not because we can track it but because it looks
  similar (identical?) to shadowmeld and needs to prevent vanish inference on
  night elf rogues.

- ZKS simulation doesn't know how to create nonAuraEvents

- don't push duplicate time stamps for evidence (some abilities like bubble taunt
  generate 1 event per mob taunted (maybe)). allows reducing buffer size

- generalize Event to allow non-tracked characters to be event sources
    - happens when CC is used: the source is the target (or multiple targets, for AoE)
    - for AoE CCs: need to prevent adding tons of events for a single ability (one
      AoE cast = possibly many mobs fire UNIT_AURA->events)
- fold evidence tracking into Character

- fix the fiery brand 2-charge problem
    - requires a side effect-free inferAbility call to see if an update is
      plausibly an ability use (check things like button press, cooldown
      state, etc.) without clobbering `buff`'s state to allow inferAbility
      to run (i.e., startTime=now, certain=false).

- don't clear tracker data when party members join/leave/change positions
    - requires migrating current CD status tracking into Character and out of the
      icons that display the CDs. one reason this isn't trivial is polling: the icons
      register OnUpdate, so are constantly checking for changes, e.g., when a recharge
      completes

- UNIT_DIED handler. Seems the GUID is secret - probably fires for deaths
  of NPCs as well as players. PLAYER_DEAD also fires, has no payload. would
  be better than UNIT_DIED if that includes NPCs
    - combine with UNIT_FLAGS-based combatDrops

- Detect ability resets. E.g., detect when cold snap is used to reset ice cold/
  ice block.

- Minimap button
    - re-anchor frames to deal with sort changes
    - reset cooldown trackers

- Options
    - Font options for cooldown numbers
        - outlines, size, font
    - Support directional layouts other than horizontal+left of frame
    - Max history tray size
    - Show offensives on right
    - maybe add tooltips for confusing abilities like the DH apex talent
    - Options to augment the centerdefensivebuff icon:
        - Blizzard opts not to show buffs some would like to see. E.g.,
          bubble, evasion and meta.

- [PROBABLY LATER] Major debugging feature: export string with all trace data from e.g., a dungeon.
    - [In the short term, ignore everything below] Compressing data into a shareable string is great for
      debugging, but in the near term, an uncompressed record with an explorer will be very useful. The
      calculations below indicate that storing inference records without compression should be no problem.
    - Some ideas about compressing inference history into a shareable string
        - layout: one block per target guid, so every record in that block can be assumed to target
          the same guid without wasting storage space. when caster != target, encode guids
        - header: build guid encoding (e.g., slot indexes 1..5 in 3 bits).
        - inferAbility: make bit string of all logic layer and confidence network results
          for each inference call.
            - [maybe] record large data like timestamps only for failing logic elements or passing network elements
            - run length encoding for multiple inference calls made on the same data changes
                - how to deal with duration changing?
            - time stamp encoding? 4B=32b float probably. can an offset encoding help? if dungeon < 40m is the longest
              tracker, 40*60*1k is ~2.5M ms is the max offset, needs 22 bits. so 68% compression. maybe helpful.
        - game loop: store bit-compressed records of every aura with an interesting flag and a bit on
          whether they were correctly inferred. ~8 bits of flags, 3 bits for slot (5 man content). header flag can
          help 
        - quick and dirty calc: worst case is probably prot paladin with ~10 tracked cds, 1 CPM. so 30 min
          dungeon would be 300 inference requests. assume avg 3 inference attempts per success = 900 traced
          events. 10 bytes per inference (how realistic is that? one time stamp is 4B) = 9kB, so base 64 encoded
          would be a 12,000 character string. Not that bad.
    



NICE-TO-HAVE FEATURES
===============================================================================

IDEAS TO IMPROVE DETECTION OR EXTEND TO OTHER ABILITIES -----------------

- ARCANE SURGE ID. hard to do since mirror images is identical duration/flags
    * MANY talents affect surge
    * PLAYER_DAMAGE_DONE_MODS?
    * required sunfury talent gives spell haste that procs a unique event
      (UNIT_SPELL_HASTE)
    * optional sunfury talent extends duration
    * causes (i think?) a massive number of mana updates through UNIT_POWER{,_FREQUENT}
      because it drains almost all mana which is then replenished quickly
    * mirror images doesn't generate any other buffs (maybe - could some talents change that?)
    
- could record exact duration extends. since these extends always fire updates, it could be
  possible to accurately calculate the extended duration rather than just saying GTE (though
  this is not trivial - sentinel updates every 1s, proc>avatar would update avatar button press..)
    - would require button press evidence+update
    - but is this useful at all?

- cooldown backpropagation: comment from infer
    - let cooldowns back-propagate. e.g. bubble and wings cast close together: bubble 1st
      is not IDable. wings 2nd (during bubble) is IDable b/c of lack of debuff. once wings is
      IDed, it will be entered into the CD tracker, but starting after bubble. so we can infer
      that wings was not the button press at the time of bubble, but the current method of
      detecting off-cd abilities will still (correctly) say that wings was available when bubble
      was pressed.
        - the logic needs to be: IF the ability was pressed then, would that violate other known
          CD info?

- the sacred weapon buff from wings is RIC=1. maybe helps ID.

- after learning that RAID doesn't apply to others: ideas for additional evidence:
    1. freedom (with talent): applies to multiple people. it's rare for one ability to apply 2 important
       buffs. though there's no direct link between the two buffs (diff IDs) other than proximity in time
    2. sac: shows a big center icon. could be a bad idea since i'm not sure how center big icon priority
       works. i.e., if the target already has a big defensive running, would sac always overwrite it?
    3. divine toll: a zillion absorbs when used in aoe
- other WoW events to handle?
    - UNIT_THREAT_LIST_UPDATE?
- nameplate personal buffs/debuffs
    - this could be a massive information source for IDing buffs that we'd like
      to track but didn't make it onto blizzard's IMPORTANT/BIG/EXTERNAL flag set.
    - need to test an ability that we wish could be tracked but isn't (like fort brew?)
- detect abilities that reset other cooldowns like adrenaline rush and ice block
    - should not be hard. if the ability has a fixed cooldown (i.e. no CDR),
      then seeing that ability before it is available means the reset was used,
      so infer the reset ability. can track the CD of that.
        * would not be able to track the reset ability if it was used to reset
          something else. e.g., if prep was used while adrenaline rush was available,
          the prep usage would not be noticed. but that's fine.
- [maybe] add CCs/stops if the blizzard CC flags cause uniquely identifiable events
- do cheat death events generally fire IMPORTANT events? rogue, evoker, DH, warrior, mage..
- could hunter exhil be detected by the idea that they only have one
- GoAK might not be cancellable? check. helps identify.
    - it isn't. right clicking and /cancelaura do not work
    - however filter=HELPFUL|CANCELABLE is the same for ardent defender and GOAK,
      despite AD being cancelable (right click and /cancelaura)
- self-cast heal that requires a button press (i.e., isn't a proc) and also
  applies a hot? how to distinguish from health pot?
    - buttonPress
    - UNIT_COMBAT heal where the caster=target=a hunter
    - UNIT_AURA applied a HoT (basically required talent)
    - is SPELL_UPDATE_COOLDOWN sent to party? or only fires in the player's client
- more work to distinguish divine protection from wings
    - some wings constantly update themselves (sentinel), though in that case there
      is no DP..
    - maybe this can work by adding inference on update? some of the wings versions
      fire update events and the ability can then be identified
    - however, for that to make DP show up as a center defensive the update inference
      logic would be that updates _MUST_ occur for the spell. how would DP know that
      it didn't receive an update and therefore should fire?
- [NOT SURE - this is a valid idea, but it is very rare to be unable to ID abilites
   after their duration completes]
    add logic layer to detect when all possible CDs have been cast
    - how useful is this? suppose at time X we know every ability is on
      cooldown. then when the next ability is used, can assume the best case
      scenario for every ability (min CD across all abilities).  this could
      eliminate some possibilities.  e.g., DK uses AMS at time=1 and IBF at
      time=10.  so at time=10 both are on CD.  the min CD across the two is
      40s, so until time=10+IBF's cooldown, anymore abilities from that player
      must have been AMS.
- confidence layer -> confidence network
    - the abstract idea for confidence should be that of a network. each confidence
      method should state its level of confidence and the decided ability, then the
      network should check itself for consistency.
- could we do better for the dreaded >1charge+CDR abilities?
    - the current behavior is to ignore CDR entirely. when used with full charges, the
      cooldown swipe begins and it propagates itself at the full CD timer. it is assumed
      that the next recharge doesn't start until the first recharge is completely full
        * can do better than this. each time the ability is observed is extra information
          that can help bring the timer closer to the truth.
        * currently the events are entirely ignored. the code just ensures that the
          numQueued counter never passes the max number of charges
- is it possible to track cold snap? can we infer that it was used when some other
  ability we can track comes off of cooldown unexpectedly? e.g., the ideal case would
  be when a mage casts spell 1 (unknown) immediately followed by spell 2 (inferred: ice
  cold) while ice cold was known to be on CD.
- get data to make better estimates on duration solve tolerance
    - both the "match" epsilon and the minimum difference
- use updating to ID spells
    - e.g., fiery brand, sentinel constantly self-update
- better handle CDR
    - can compute upper bounds (or averages) on the amount of CDR gained. e.g., throwing
      avenger's shield on CD at 5 targets = 5s CDR per 12s (though procs reduce this
      and so does divine toll).:



ARCHITECTURE FEATURES ----------------------------------------------------------

INFERENCE ARCHITECTURE (maybe)
- instrumenting inferAbility suggested an Inference class that manages both the
  execution and record keeping for each inference could be beneficial.
    - replaces InferenceRecord
    - would store much of the data used and offer various ways of viewing/exporting it

ABILITY ARCHITECTURE (maybe)
- Abilities may need a formal object. Not yet convinced about this. This could be
  the solution to another arch issue about moving cooldown information out of the
  UI elements (swipe trackers)
- EVENTS map one-to-many to ABILITIES

- (ARCH) SUMMARY OF UI FEEDBACK
1. claim ability was used
    a. TTS announce
    b. glow the UI element
    c. track the CD
2. claim the ability is done
    a. unglow UI
    b. start the cooldown swipe

- fold eventTrackers, cdTrackers and buffsForInference into Character.
    - buffsForInference: consider generalization to kicks, stops
    - empty eventTrackers and cdTrackers need to be easily creatable for ZKS

- could buff removal become an event for eventTrackers, further generalizing the
  event architecture and removing special logic in UNIT_AURA?

- [NOT SURE - is this really helpful?] better non-fixed duration support: generalize
  current FIXED, LTE, GTE system to a duration range (min, max) where the default
  FIXED idea is implemented as (duration-TOLERANCE, duration+TOLERANCE), LTE is min=0,
  GTE is max=Inf. some abilities surely have extensions that are short enough that this
  helps distinguish

- explore the empty character idea for getTrackedCharacterByGUID:
    when guid=nil or the guid isn't found (shouldn't happen), return a special
    empty character. if the CD tracker, various states of cooldown swipes, and
    even textures in the history tray get folded into Character(), then the empty
    idea is way more elegant. need to check all 4 use cases of getTrackedCharByGUID
    to make sure it would work:
        1. UNIT_AURA
        2. updateSlotToGUID
        3. updateSlotToGUID
        4. updateStaticRow
        5. updateHistoryTray
    the main problem is uniqueness of the empty GUID. things keyed by GUID would
    not like it (unless we generate many empties?). e.g., TrackerUI:GUIDToIndex
    ---- BIG PROBLEM ----
    inferAbility expects an object that collects all CD trackers together for inference.
    this can be changed such that abilities (who know their caster) can refer to the
    appropriate cooldown tracker, but will take a little work to allow an empty
    tracker to be substituted in that case.

- [LOW PRIORITY] multi-charge abilities and the history tray: this bug only
  exists someone disables the hide inaccurate trackers option between two
  casts of a multi-charge CDR ability. the bug shows a cooldown swipe for the
  sum of the two recharges, not the current recharge time.
  * the first ability goes to the history tray and does not call queueCooldown
    but *DOES* trigger the cooldown tracker. the second goes to the tracker via
    queueCooldown, which has numQueued=0. but because the first ability triggers
    the cdTracker, the endsAt time knows when the second charge will end and
    sets that as the cooldown duration.
  * interestingly, the startTime in this case is in the future, starting at
    the actual beginning of the last charge's recharge. the swipe stays at
    12 o'clock until the time passes future-cooldown, then begins and ends
    at the correct time for the second charge.

- groupSolutionUI
    - doesn't scale with non-5 member group tracking (raids/arenas)
    - isn't wide enough to handle things like groups with multiple paladins
    - doesn't show casters, so a multi-paladin group would have multiple freedoms
      but no indication that they are cast by different paladins


OTHER FEATURES -----------------------------------------------------------------
- consider LibCustomGlow for shine

- visual on solver for abilities that can't be solved under ideal zero knowledge
  conditions (e.g., arcane mage surge and mirror images have identical flags
  and durations).
    - highlight these icons red

* ability database completeness is questionable
    - is looping through all spell IDs and dumping any with BIG, IMPORTANT or
      EXTERNAL flags exhaustive?
    - i only looked at all C_Spell.IsSpellHelpful()=true spells that were either
      BIG|IMP|EXT. be sure to dump not_helpful_spells when building the db.

- inferAbility() - save the conflicting spells/casters for UI elements




LIMITATIONS
===========================================================================
- COOLDOWN TRACKING ONLY WORKS FOR GROUP MEMBERS WITH LibSpecialization.
  Players without LibSpec will only be tracked using the history tray mode.
  LibSpec is embedded in common addons like bigwigs (and this addon), so many
  people have it installed without being aware of it.

- Delayed inference: some classes have abilities that can't be distinguished
  until some time passes and more data can be collected. Despite not being
  guessed immediately, these abilities will glow when confidence is attained.
  This cooldown tracker does not believe the ability started at that time and
  cooldown counters are based on when the buff was applied, not when inference
  occurred.

- Fiery brand spread and multi-charge talent: the spread and duration extending
  talents cause the ability to update itself. When combined with the +1 charge
  talent, it becomes difficult to know what happens when the ability is updated.
  Did it spread? Did an immo tick extend duration? Or did the player cast the
  second charge? There are ways to guess, but they are not 100% accurate.
    - Technical answer: need a no-side-effects version of inferAbility.

- Weird cases: like multiple paladins. Are their externals always different
  buffs or might they update? Sacs must be different since they deal damage
  to the casters, but what about freedoms?

- Abilities with dynamic cooldown reduction (CDR) are hard to handle. This means
  abilities that reduce their cooldown due to some event - for example, a guardian
  druid's Incarn cooldown is reduced by 1 sec for for every 25 rage spent. Since these
  events can't (generally) be detected, the cooldown will show the MAXIMUM POSSIBLE
  (worst case) cooldown. This information can still be useful, but judgment is required.
    - For these reasons, there is an option to disable inference for abilities with
      dynamic CDR. They will instead always be sent to the history tray rather than
      display an inaccurate timer. This instead shows you **how long** it has been since
      the ability was last used.
    - A much bigger problem: abilities with **both** CDR and multiple charges. For
      multi-charge abilities, only one charge recharges at a time. If there is no
      dynamic CDR, this is no problem to track. But if there is CDR, consider the
      case where both charges are used back to back: because of CDR, we don't know
      when the first recharge finishes (equivalent to not knowing when a 1-charge
      ability with CDR comes off cooldown), which means we also don't know when the
      second ability **begins** charging. This compounding issue makes the cooldown info
      very difficult to judge.
        Rotational CDR (definitely not trackable)
        - Disc priest: pain suppression
            - CDR on PW:S casts
        - Prot paladin: GoAK
            - CDR on holy power spenders
        - Prot warrior: shield wall
            - CDR on rage use and shield slams
        - Vengeance DH: metamorphosis with the Annihilator hero talent and apex talent
            - CDR on third voidfall meteor
        CDR on large cooldowns (e.g., resets)
        - balance druid: incarn/celestial alignment
            - keeper of the grove talent that reduces cooldown based on time spent
              with ability off CD.
        - aug evoker: scales
            - potentially trackable since the CDR is attached to a long
              cooldown that may itself be trackable


KNOWN BUGS
===========================================================================
- Cooldowns that are active (glowing) while changing zones will never stop glowing
  until used because the aura event either doesn't fire while loading or the
  handler is not active.

- double freedom

- Death handling is not implemented. For now, buffs that are running **and**
  not inferred when the unit dies will be lost. This behavior will be fixed
  in a later update.

- Changes to party sorting order aren't detected. If you change your
  party sort (e.g., edit mode > party frames > sort), a /reload is necessary
  to properly attach the cooldown trackers to your frames.

- Follower dungeon forced respecs: when entering a follower dungeon with a role
  that doesn't match your spec, the game forces you into another spec. E.g., if a
  prot paladin queues for a follower dungeon as a DPS, the paladin is force-
  respecced to ret. LibSpecialization does not detect this change. A /reload will
  resolve the issue. This will happen again when leaving the follower dungeon.




DONE (not necessarily in order of completion)
===========================================================================
- why is Character(player): new character called so many times on simple UI reload?
    - it isn't. the new character log message was accidentally moved into setBasicInfo. fixed.

- force disable non-working abilities
    THESE ARE JUST SET TO DISABLED BY DEFAULT AND RED TEXT EXPLAIN WHY
    - better to say "more abilities coming soon!" than to include difficult, buggy abilities and
      make people think it's a mess.

- multiple icons in tray for 1 ability use:
    SHOULD NO LONGER OCCUR DUE TO THE numAuraProviders=1 EXCLUSION
    - pillars
        * many talents extend pillar duration
    - AMSes in tray
        * AMS naturally updates. maybe when the immunity aspect is broken or
          a rider of the apocalypse proc

- extend frame/slot tracking beyond the hardcoded 5-man party system
    1. move cachePossibleAbilities out of trackCharacter
    2. better separate maintenance of roster data from UI state
    3. extend beyond 5-man m+ groups
        - remove "index=1,5" loops
    4. fix trackerUI. moving from stable indices back to unstable slots means update() will have to
       do more work.

- respondToRosterUpdate() needs to do more data maintenance
    - needs to run cachePossibleAbilities. currently is only done on the initial trackCharacter(),
      which makes no sense, and after libspec gets talents.
        * this needs to be removed from trackCharacter
    - probably also a good time to update data trackers like evidence, cdtracker, slot/guid/etc maps

- more explicitly delegate code to determine which GUIDs/slots are "tracked"
    - GroupData.lua: trackedCharacters[] is the list. maintained exclusively by trackCharacter()
    - trackCharacter: in updateSlotToGUID and libspec callback

BIG PICTURE: need special handling for AURA(update) events. required concurrent events will
only happen on cast, not on update, making updates look like other abilities. however, when
a new ability really is used, it should have all of those concurrent events on update.
    - could numAuraProviders=1 be the easy answer? if =1, then that ability cannot have been
      cast on an update.
    - added this, but should also propagate to the other events that this aura was updated. the
      numAuraProviders=1 check will prevent the update event from wrongly triggering freedom, but
      if the main event knew an update happened on this aura, it could also exclude freedom and ID wings
- new endless Event bug in manageEvents:368
    * maybe it's double freedom
    * seems it was wings. see video 3:34
        - wings>toll at 3:17
        - 3:20 wings updates and gets IDed as freedom
            - the ID as freedom is because there is no concurrent buff when the second update happens
            - should be able to tell this isn't freedom because freedom doesn't update
expiring batch of size 2 for id=aura175
aura instance ID aura175 added to history tray party1 after 18.013999999s
    * the error is blocking evBatch[1] in the expiration loop. fixed.
        - but maybe the expire loop should be a separate one from the infer loop..

- sentinel never stops glowing
    - maybe not reproducible

- leaving follower dungeon with solo unchecked still doesn't work
    - see recording
    - why does it open to addon options? [nvm]
    - becauser respondToRosterUpdate() is always called in the loader handler
    - ok, the recorded problem happened because ZONE_CHANGED_NEW_AREA was not unregistered,
      but this whole system will not work. zone changed/roster update need to remain registered
      at all times so the addon can detect changes to group/instance state
    - new changes may have fixed it. let's see.
    - [SOLVED] but the solution is messy because there isn't a clear separation between data
      collection, UI updating and :Show()ing. the biggest offender is the explicit :Show() loop
      in respondToRosterUpdate()

- behavior to solve with updates/batch inferences
    - MUST DO: ensure new batch inference doesn't trigger feedback on every certain inference
        * it seems this is the most natural design. AURA(update) events are always created (even
          for certain inferences - multi-charge abilities must do this), then data sharing through
          a shared aura object implicitly incurs "batch" inference when inferAbility() is called
          in sequence across events in a batch.
    - good test: sentinel - is a self updater that doesn't instant ID because of bubble
    - if an aura update arrives before a certain ID (e.g., in this case because we can't rule out
      bubble until no debuff is observed in the tolerance window), an update event is added to
      sentinel's batch. later, when the first AURA(add) event is IDed with certainty, it is no
      longer the event checked by the AURA(update) handler. instead, the update event is. the
      update event will never ID because the original add event put sentinel on CD.
        * the result is a cascade of update events that trigger because of the second update
          event being uncertain.
        * this now rarely happens because of the tolerance tail re-evaluation, but it is still
          possible and the system should be built to handle this
    - the answer to all of the above was to remove events from the queue when there are 0 possible
      solutions under permissive evidence filters

- UNCERTAIN spam
    * only show on first uncertain inf

- as i guessed long ago: the RAID flag is only 1 for the player that casts the ability. it
  seems RAIDINCOMBAT does not require this. see: freedom is RAID=1 but shows as 0 when another
  paladin casts it and sac is RAIDINCOMBAT=1 and remains =1 when another paladin casts it.
  when i cast ironbark (RAID,RIC=1,1), both are 1.

- abilities that apply other auras
    - general confidence function
    - *consistently* glow otherAura's cooldown box
        - right now the redirection is only honored if there's an uncertain inference (i.e.
          both abilities are off CD and can't decide between the two). e.g., if avatar is on
          CD and AotS procs, then AotS will be inferred with certain=true and will not glow
          avatar.
    - don't clobber the other aura's cooldown swipe/timer (probably does)

- enable/disable option
    - enable solo
    - enable in 5-man groups
    - always enable (UNSAFE)!
    TESTING
    - toggle enable/disable while solo
    - toggle enable/disable in fol dung
    NOTES
    * hide UI elements
    * delete all character data/internal trackers (?)
        - :untrack()ing all tracked characters
        - delete ns.cdTracker
            - is another good reason why ns.cdTracker should live in Character()
        - force-expire all events
        - check all tracked data in ns.*: make sure deleting the above removes everything
        * to recover from this, must manually send LibSpecialization.RequestGroupSpecialization().
          this shouldn't be done in the normal code, so maybe generate it when the option is checked?

- debugging options
    - talents
    - inference
    - verbose (rename muteverbose -> enable verbose)
        - has multiple effects: talents+verbose, inference+verbose

- only enable in party
    - raid/arena support (whatever it will be) can come later

- These bugs were caused by not appending realm name to character names. This prevented
  playerName -> slot mapping in the libspec callback.

    * Bugs on loading into dungeon. first frame they are already present. looks
        to have something to do with querying character information too early.
        messages on console show Character() constructors with everyone properly
        named on frame 1 after loading.

    1. this one happens during the GROUP_ROSTER_UPDATE/PLAYER_ENTERING_WORLD handler.
        was only one character in the tracker when this was thrown.
        (3x over the whole run)

        GUID is valid yet name="Unknown", className=classFile=raceId=nil for slot=party2 in a real run
            GroupData:82: classFile is nil
            ns:printDebug(string.format(
                "Character(%s): new character GUID=[%s], name=[%s], class=[%s]",
                slot, GUID, name, classFile))

    2. this one happens during the libspec callback's trackCharacter(). slot=nil
        (6x over the whole run)

        function ns:trackCharacter(slot)
            local guid = UnitGUID(slot) 

    3. new error for the same reason. prev errors didnt happen
        groupdata:110: attempt to index local 'coldata' (a nil value) in getClassColor
        groupsolutionUI:92

- for debugging: show the reqs met somewhere in the final inference string
    - e.g.: reqs[buff=<diff>s|cast=<diff>s] etc.

- Aura update rewrite using events
    (NOTES CONSOLIDATED FROM ELSEWHERE)
    1. update loop: GOAL: detect whether new ability caused this update
        -> new ability occured that caused update?
        -> more information came in about an uncertain ability
    - update loop: if there's a reason to associate a new ability, append to list
        - things like finalize need to happen here so that cooldowns are known ASAP, not
          when the buff expires (affects potentially other abilities)

- new event on update system implemented for most trivial cases
    - no code uses naturallyUpdates anymore. sentinel is a good test case
- batch-resolve events attached to the same aura
    - first: store aura-tracked events as a list per auraID
- [NEEDS TESTING] concurrentBuffs: if the buff that must be applied by an ability that can be updated,
  these currently will not fire. need to check the updated list in UNIT_AURA while
  pushing into eventTracker.buffs
    * e.g., thunder blast from avatar
- bug squashing during the event framework rewrite
- massive aura spam 1000s of attempts
- continue decoupling event from buff
    - removing "buff" from most everything in PetesDefensiveHistory.lua
- remove lastNegativeAuraId/makeAura hack for non-aura events in shadowmeld
- retest meld since it exercises a completely different code path
- continue removing event>buff.duration
- test removing infer from buff added
- get rid of all external uses of ns.eventsForInference
    - move poller loop into Event.lua
- generalized event system
    - make inferAbility not depend on buff-specific stuff
    - events have:
        - basic info (most of above)
        - evidence (current event trackers)
        - whether the event is flagged to be untracked
    - remember:
        - want to be able to query evidence when deciding to make an event. examples:
            * meld: drop combat + button press + aura
            * updates: button press + aura update
    - generally: getEventTimeDiff/ConcurrentEvent(): these are about evidence, which
      should be handled by Event(). the later judgment and weight of evidence by the
      confidenceNetwork will *NOT* be delegated to Event.

- SHADOWMELD: support non-buff events. Although shadowmeld creates a buff, it is an
  unremarkable buff so it would be hard to trigger an event from it. Instead, trigger
  a "tracked event" when a character of interest leaves combat. This tracked event
  will occur no matter how the player left combat (death, enemies died) and will be
  kept around for inference for 0.5s. If, in that 0.5s, other evidence is gathered
  that indicates shadowmeld, good for us.
    - Events for shadowmeld: UNIT_AURA, UNIT_FLAGS (combat drop), SPELLCAST_SUCCEEDED
    - This is a general framework that can support kicks and CCs as well

- extendable abilities
    - can the one buff=one ability system be extended to one buff=>0 abilities?
      some important constraints
        - did the buff update? should this buff update?
        - did the update coincide with a button press? does the update require a button press?
            * e.g., sentinel stacks update regardless
        - should be flags specifying the buffs that can be affected by multiple abilities
    - THE BASIC IDEA: allow removal-like end of life when inference happens in an update.
      generally, abilities with charges should admit this with special flags for handling
      meta and goak.
      THE CHALLENGE: abilities that can't be instant-IDed. if the ability needs to run to
      its duration to be IDed, the update itself needs to be leveraged as IDing info. but
      as usual, using information for one ability means we need the information for all
      abilities, so requires another ability pass to see which update. some will update
      with concurrent button presses (dynamic extended abilities like sentinel)

- vampiric blood: the coagulated blood thing is not being correctly tracked.
  think it should be fixable. just look.
    - two things: coagulated blood is an example of an aura that is returned
      by neither HELPFUL nor HARMFUL filters.
    - it is also probably not related to vamp blood, it is the tracker for how
      much damage should be healed by death strike. it is probably showing up
      because i'm testing these things out of combat.

- even after improving, it still might be a good idea to add an option to disable
  inference for CDR+charge abilities, sending them to the tray instead. maybe one
  checkbox that affects all such abilities (and dynamically determines if the needed
  talents are chosen).

- associate player ID token with rows so that on GROUP_ROSTER_UPDATEs we can
  potentially preserve data. very low priority - just clearing and resetting
  all data should be fine for real world use.
    - currently using player name from libspec

- stop delegating data management to the icon rows.  logic is all duplicated due
  to the tray+cd bars.

- do absorbs cause UNIT_HEAL events? could help ID AMS, ret pal DP
    - yep, it fires. but not a heal. is UNIT_ABSORB_AMOUNT_CHANGED(slot)

- rearchitect groupCDs -> groupData
    - loop over group
    - setGroupData (name, GUID, class, classFile, spec, talents)
    - getGroupData (same as above)
    - derived data getters:
        - getAbilities(castableOnly=true)
            - castableAbilities[slot]
        - getTalentRanks()

- the mapping of party1/party2/.. can change when a group roster update occurs.
  this means objects that store references to who cast them/who they were cast on
  should not use party1/2/.. IDs. to handle this, add a translation table for
  slot -> GUID to get unchanging unique IDs. this translation should be used for
  character data (like casters, talents, names, etc.) but NOT for things that are
  fundamentally party slot based (like the trackerUI elements).

- addon options
    - disable history tray
    - disable all inference (@Aryella WoWUIDev)
    - text sizes
    - icon size
    - icon padding

- enable TTS
    - add option for saying ability on instant ID

- rearchitect for multiple inference to handle concurrent buffs or debuffs
  (bubble, bop, spellwarding) possibly occurring in other events. this also
  lays the foundation for monitoring updates, revising inferences, etc.

- [NEEDS TESTING] ripped out useDuration from inferAbility, instead if endTime
  is unset in buff, it is assumed the buff is not yet over. the DURATION mode
  is then forced to LTE to allow the buff to be less than its stated time, but
  having the effect of excluding other buffs with duration > the current time
    - what to do about inferAbility(useDuration=true|false)? it's not that simple
      anymore when considering updates. since useDuration often requires exact
      matches, i guess it should be false.
        - the fix to instead make fall back to DURATION_LTE when 
          the buff's stated rule? would that work for VDH apex vs. meta? (it would)

- paladin: ret bop caused a 500s cooldown with a swipe that never started moving
    - this is despite being listed as 240s on /pdh
    - it was also instant IDed
    - bug triggered bop/spellwarding CD hack

- handle shared cooldown between BoP and spellwarding

- issues related to concurrent buff/debuff that should be solved by multiple inference:
    - spamming shadow dance somehow caused dance to be excluded due to (I think)
      lack of required buffs??
    - spellwarding doesn't work when cast on self due to the concurrentDebuff issue
    - bubble doesn't work when forbearance is already on the player (e.g. spellwarding
      into bubble)

- generalize the cast tracker to any event
    - remember to add relevant events in ZKS
    - this solves the long standing bugs with forbearance abilities that
      could apply their debuffs in different payloads under some circumstances
      that were hard to predict.

- VDH meta cooldown is forgotten when apex talent gives meta buff

- [FIXED] solve on-login bug. caused by LibSpec's handler firing before the first
  UnitExists()=true is witnessed by our handlers.
    - the fix was to check for both UnitExists and ns.slotToGUID[slot] != nil in
      the relevant locations. this was encapsulated into a new function
      getTrackedCharacterBySlot() that generalized to even more use cases.
    - this also fixed a bug thrown when zoning into follower dungeons

- ensure libspec callback Character() doesn't overwrite an existing one.
  though that is probably ok since the GUID doesn't change and the libspec
  one will be reinitialized to an empty slate..

- ns.allSlots purging
    - remove common if not allSlots[slot] then return pattern. replace with
      a check on tracked characters

- merge the separate history tray/static row/debug labels into a single UI element
    - many of the frame ordering bugs were caused by only maintaining/updating the
      history tray in GROUP_ROSTER_UPDATE. rather than fix this oversight, this is
      a good time to get rid of some technical debt.
    - frame ordering
        - when libspec's message comes through, order is corrected

- group update seems to be broken again (see: follower dungeons)
    - empty members
    - reanchor
    - part of this: consolidate the static row, cooldown tray and debug labels
      into a single frame rather than two separate rows anchored to opposite
      corners of the party frames.

- mark CDR abilities so users know the cooldown is only a maximum

- add option to remove charge+CDR cooldowns from the tracker row and only show
  them in history tray, due to difficulty in tracking them

- when leaving party, currently active cooldown swipes stay in the old UI location
  e.g., ret pal in follower is party3 -> leave party becomes party1, new CDs are
  populated in party1 but with no swipes and party3 has continuing swipes
    - need to fold historyTray and staticRow items into Character() to do this

- for testing cleanup after group changes:
    - leave instance group: leaves all working data in slot 2
    - respec ret -> prot: does not remove old spec abilities from static tracker
    - bubble didn't work (prob due to req debuff), but did not get sent to history tray

- [FIXED: cached abilities happened before follower dungeon tracking] bug in follower
      dungeon: some externals aren't considered in cached possible
    - to reproduce, queue as prot into follower dungeon as ret. cast sac or freedom
      on party. should not be detected. doesn't matter who it's cast on.
    - then respec ret > prot and try both casts again. freedom works, sac works
    - but do they work the second time because the respec fired the libspec handler?
      or did something break internally when prot became ret without an event?
    - OK: queued as ret and it still didn't work. something is wrong with the
      cached possible abilities and it is being fixed by respec firing libspec
        * just to confirm, respecing WITHOUT reloading fixes, so it is just the
          libspec update

- cooldown swipe is not black for bear incarn see end of magisters run. does
  bear incarn have charges?
    - no, the new charge tracking code broke cooldown swipes for abilities with CDR
- support danders frames
- addon options: why do people still have to swipe the size sliders? seems
  like nil is somehow the default for iconSize
    - this doesn't happen on fresh chars. i don't see how it can happen for
      old saved variables either since all of the getters return X or [32|15|3]
    - it's because the HistoryGrid code accesses the table directly and we don't
      wait for ADDON_LOADED
- option to hide history tray items at max CD
- history tray items aren't hidden when their max CD completes (maybe this should
  be an option? useful for debugging)
- remove concurrentDebuff "case 2":
  concurrent debuff not allowed prevented astral shift on shaman during real
  run. debuff happened to be applied in same tick, this probably happens
  often.
- how to deal with hero talents always being active?
    - the root talent is always selected=T, purchased=F
    - other talents are always selected=T, purchased=T if the player has ever
      filled out that talent tree
    - good test case is marks hunter: 2s trueshot duration applies no matter what
      hero talent is active
        * better test case would be a hero talent with a choice node
- better handling/inference for abilities with charges
- talent handling 
    - FULL PASS ACROSS TALENTS AND SPECS: migrate to AbilityDb from SpecAbilityDb
        - recapitulate the old spec structure of the database through hasAbility
          talents attached to class- or spec-specific talents
    - extracting talent selections from talent string
    - support removing alternative spells from talent list when only
      one of two can be chosen (e.g., balance druid celestial alignment and incarn)
    - accumulate additive and mult modifiers so that they can be
      applied to base values
- toward multiple inferences (e.g. for spells that update):
  the stuff below also fixed a corner case where non-certain initial inferences
  (right now, only metamorphosis) behaved incorrectly if the second inference
  did not find a confident match.
  add a "working guess"/uncertain state. isAbilityInferred() does not
  currently work correctly for !certainOnFirstInference abilities (meta),
  because it only tests whether the buff is named. this *happens* to work
  when the second inference is always successful, but in real world use there
  will be times when this fails. currently, uncertain inference only applies
  to meta and does:
    1. turns on meta's active glow when an uncertain inference is made
    2. at expiration, run inferAbility() again, but even if this fails it
       accepts the buff and runs finalize() because isAbilityInferred()
       doesn't check the uncertain status
    3. sets a cooldown swipe for the cross-ability maxCD on meta
    4. turns off the active glow on meta
  how this should work is:
    1. on apply: run infer()
        1a. infer=true|uncertain: trigger active glow
        1b. infer=true: finalize
        1c. infer=false: do nothing
    2. on expiry:
        2a. infer=true|uncertain: remove active glow
        2b: infer=true and wasn't finalized: finalize
        2c: infer=true: start cd swipe
        2d. infer=false: add to history tray
- DOESN'T WORK WHEN SOLO
    - libspec doesn't fire a second time, so static rows aren't populated
    - even requires /reload after joining a group
    - gives this error when it happens
        PetesDefensiveHistory.lua:194: attempt to index field 'items' (a nil value)
- get rid of external=enum { NOT, ANY, NOT_SELF}
    - can't get rid of this because freedom is not flagged EXTERNAL. instead,
      generalize the idea to "targets"
    - the only example of this is sac, which is better handled as a sac-specific
      logic layer, not a flag on EVERY ability in the database.
- revisit the comment about separating out externals in updateGroupData
- get rid of certainOnFirstInference. this is now a confidence layer thing
- add tooltips for chubb
- updateRow
    - e.g., when specText or cpfMapping change, update the associated
      text widgets rather than forcing the functions that update those
      values to do it.
- on death: clear active cooldown glows for all static icons
- ensure history tray is not broken after adding options for icon sizes
    - specifically, make sure anchor point on bottom right is updated with
      icon size and that size/anchoring is separate from alloc
- curseforge build hook
- add iconId for all database spells for static tracker
- for instant tracking: flag spells as static icons when they're identified
  by the 0-knowledge system and use that flag to decide where to send the cooldown
  icon.
* checkbox option interface for choosing which spells to track per class
* database completion tasks:
    - ALL raidFlag and raidInCombatFlag values need to be checked! cannot dump them automatically
    - continue filling out spec ability database
* add logic layer for DH metamorphosis and new apex talent
    - special case where these two abilities both proc meta but can be
      distinguished by duration. so the logic layer would say: if there are
      only two possible remaining ability matches, if those two are only meta
      and apex talent, then light up the meta tracker no matter what. at the
      end of the buff, decide whether to start a cooldown timer.
    - adding this logic layer requires changing our mind about a buff inference
      at timeout. this could be done, but requires some rearchitecting. almost
      every part of the inference process is clean (meaning running another
      inferAbility wouldn't do anything wrong) except for:
        1. buff.caster
        2. cdTracker
      cdTracker is the worst problem for two reasons: (a) it is clobbered when
      the first inference happens meaning old CD info is lost and (b) it is
      supposed to be useful for OTHER inferences. the elegant way to handle
      this is to add an ability option to prevent tracking on first inference.
      should also move cdTracker maintenance out of InferenceEngine to keep
      reponsibilities cleanly delegated.
- do more setup, layout and allocation when the addon is read in rather than
  waiting for events. obviates many annoying checks and firms up the
  interaction with LibSpecialization that occurs on login before group comms
  begin.
- hide cooldown swipe on active static buffs
- the blessing of freedom problem: it's an external castable on anyone but it isn't flagged
  as external, so any group with a paladin puts an 8s duration (1,0,0) flagged ability
  on everyone. turns out it's HELPFUL|RAID flagged and that allows major disambiguation
- stop using blizzard's CenterDefensiveBuff (just hide it) so that multiple active buffs and
  non-defensive buffs can be supported
- add spell caster inference with UNIT_SPELLCAST_SUCCEEDED
    - HALF 1: track casts
    - HALF 2: use the info
- handle instant inference in the cases where it's possible
    * for buffs that are unique without duration info
    * align these buffs next to frames omniCD style
    * these never enter the history tray
- add instant tracking
- better inference using cooldown tracking
    - e.g.: prot paladin has AD=8s and GoAK=8s so they can't be distinguished.
      if AD and GoAK are used in sequence, we don't know which was used first, but
      the only way to have 2 8s buffs is if they were both used. so now can infer
      AD is on cd and one charge of GoAK is used. so if we see another 8s buff on
      the paladin, we know it was charge 2 of GoAK and now both charges are on CD
      for some time. AD will probably come off CD during this period, meaning that
      the next CD is likely AD.
        - gets a little murkier since GoAK is also a CDR spell
- separate the bookkeeping of updatePdhGroupSolution() from the inference logic
- make a unified inference function. it should be able to work on empty cooldown
  tracking/cast tracking/etc. tables to simulate a "no-knowledge" state for the
  solutions UI
- addon namespace global cleanup
- use blizzard's filters for more info (HELPFUL|BIG_DEFENSIVE, HELPFUL|EXTERNAL, HELPFUL|IMPORTANT)
- now tracks frame sorting, properly mapping, e.g., player -> CompactPartyFrameMemberX
- don't place buff into the history until it finishes
	- possible with aura instance ID tracking
	- possible to handle the overlapping defensives where the buff icon
	  is changed before the last defensive completes
- track durations by UNIT_AURA removed list and auraInstanceID
- as new features have been added, updating the current data structures is
  too cumbersome to handle newer features.
	- new add/remove functions for history
	- new swap/clear/etc functions to fully abstract internal data architecture
	- better facilitate setup/teardown/update/reset
	- clarify data hierachy. individual cooldown frames need to know, e.g.,
	  to what player they refer. EITHER:
		1. copy all of this data into each frame and abstract away the cumbersome
		   duplication
		2. figure out how to link to a globally visible data structure.
		   this is cleaner, but depends on how wow scopes callback execution
- better guessing for:
    - duration_variable (should record whether this is < or > the stated duration)
        - in some cases fully unambiguous solutions remain. e.g.: DK AMS (<=7s) and
          IBF (=8s). if these are the only available spells for the group, then any
          duration <= 7s is definitely AMS and any 8s duration is definitely IBF.
        - in others, it becomes a best match. e.g.: paladin sac (<=12s) and just
          about every defensive. since the sac *CAN* end before 12s if the paladin
          drops below 20%, there's a chance it happens to wear off at exactly the
          duration of another defensive causing a misidentification. since the
          likelihood of this is low, the timer is still almost always useful.
            - because of these cases, it could be useful to keep an internal table
              of last-use of cooldowns. if an unambiguous sac happened (e.g.,
              a DK in a group with just a prot paladin and no other externals had
              a defensive that lasted 12s, then it was a sac) then we now know sac
              is unavailable for 60s, meaning that ambiguities caused by sac can
              be ignored for that 60s window.
- support tracking externals when it's unambiguous
    - until this support is implemented, should hide externals when they can be
      identified as it clutters the real cooldowns.
    - requires redirecting the icon from the target's party frame to
      the (inferred) caster's history.
- solving group inferences based on the full catalog of possible defensives
	- will be different per player. e.g., resto druids with 12s bark and
	  12s ironbark will never be solvable but every other group member
	  could be.
	- solution pane showing the current group members, each of their
	  possible cooldowns and which ones are identifiable
- infer ability based on duration
	- prune duplicate abilities in history when possible
- for unidentified abilities: remove the tracker text once the longest
  possible cooldown time for that player has been reached (i.e., max CD
  across all externals in the group and that player's own cds).
- handle when libspec never returns a spec
    - [UPDATE]: just requires all players have an addon (pref this one) with
      LibSpec embedded
- auras possibly being assigned to the wrong anchor?
	- case where arcane mage had blur in history?
    - [PROBABLY FIXED] older code assumed auraInstanceIDs were unique across
      characters when they might not be.
- use different textures when it makes sense (e.g., gift of the golden valkyr)

- Re-application of buffs when inference fails (or is turned off) can't be
  detected. For example, if a charge>1 ability is pressed a second time while it's
  already running, we will miss the first use if it wasn't inferred (or the user
  disabled inference). the first use can't be distinguished from a buff that
  naturally updates itself (e.g., paladin sentinel) since inference doesn't know
  what's going on. in this case, only the second use will be sent to the history
  tray when it expires.

- add list of annotated frames, which could be party/raid/arena.
    - replace for index=1,5 loops for a loop over a set of "annotated frames", which
      can be blizz compact party frames, raid frames, danders frames, etc. so long
      as each frame has a .unit member that gives a unit token like player, partyN,
      raidN, arenaN, etc.

- support verbosity levels (and checking option muteVerboseDebugging) in
  printDebug via a first argument (verbosity level or something)

